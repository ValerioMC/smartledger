<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.vmc</groupId>
        <artifactId>smartledger</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>smartledger-service</artifactId>
    <name>Smart Ledger - Service</name>
    <description>Business logic services for Smart Ledger application</description>

    <dependencies>
        <!-- Internal dependencies -->
        <dependency>
            <groupId>dev.vmc</groupId>
            <artifactId>smartledger-model</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.vmc</groupId>
            <artifactId>smartledger-dto</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.vmc</groupId>
            <artifactId>smartledger-repository</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.vmc</groupId>
            <artifactId>smartledger-common</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- MapStruct -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
